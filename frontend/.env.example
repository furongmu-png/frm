# Frontend runtime configuration
# Leave empty to use relative /api and /ws paths (works with ingress/nginx proxy)
# Set to direct URL for docker-compose without ingress
# VITE_API_BASE=http://localhost:8000
# VITE_WS_BASE=http://localhost:8765
